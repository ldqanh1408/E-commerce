server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false # Server không cần tự đăng ký với chính nó
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0 # Giảm thời gian chờ khi khởi động dev
